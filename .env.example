# JWT secret key for signing tokens
# Generate a strong random string for production
JWT_SECRET=your-secret-key-change-in-production
API_KEY=your-api-key-here

# Week 5: CSRF protection (csurf) uses cookies automatically — no extra env vars required.
# Test forged POST requests with Burp Suite; valid forms include a hidden _csrf field.